import React from 'react';
import ResetPasswordComponent from '../components/pages/ResetPasswordComponent';

const ResetPasswordPage: React.FC = () => {
  return (
      <ResetPasswordComponent />
  );
};

export default ResetPasswordPage;
